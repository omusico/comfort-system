package comfort.exceptions;

/**
 * Ошибка пользователя.
 * Это исключение следует вызывать в случае ошибки пользователя (например, неверный ввод).
 * Всегда следует обрабатывать такие исключения или помещать в секцию {@code throws}.
 */
public class UserException extends Exception {

}